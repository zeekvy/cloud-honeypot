# Used for local development without Docker.
# Docker uses environment variables set in docker-compose.yml instead.
DB_HOST=127.0.0.1
DB_USER=honeypot_user
DB_PASSWORD=honeypot_pass
DB_NAME=honeypot_db
